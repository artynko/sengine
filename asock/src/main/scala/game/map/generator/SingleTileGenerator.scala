package game.map.generator

/**
 * A generator that does nothing with the tiles around it
 */
class SingleTileGenerator extends TileGenerator {
  
   def handleNextTile(current: ModelTile, next: ModelTile) = {
	   
   }

}